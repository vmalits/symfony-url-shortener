<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                  https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\Domain\User\Entity\User" table="users">
        <unique-constraints>
            <unique-constraint name="UNIQ_IDENTIFIER_EMAIL" columns="email"/>
        </unique-constraints>

        <id name="id" type="integer" column="id">
            <generator strategy="IDENTITY"/>
        </id>

        <field name="email" type="string" column="email" length="180"/>
        <field name="roles" type="json" column="roles"/>
        <field name="password" type="string" column="password"/>
        <field name="createdAt" type="datetime_immutable" column="created_at"/>

        <one-to-many field="shortUrls" target-entity="App\Domain\ShortUrl\Entity\ShortUrl" mapped-by="user">
            <cascade>
                <cascade-remove/>
            </cascade>
        </one-to-many>

    </entity>

</doctrine-mapping>
